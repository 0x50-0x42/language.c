#define TABAR 100
#define SIZE 1000

void detab(char*, int*, int);  // detab the tabbed string

void Help();

int checkSorted(int*, int); // check whether tabstops are sorted or not

int _getline(char*, int);
